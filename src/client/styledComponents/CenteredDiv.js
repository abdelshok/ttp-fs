// CENTERED DIV
// Div to be used when we want to center text, icons, or images.
// I.e. Used within the Box component in order to center the input fields,
// submit button, title, etc.

import styled from 'styled-components';

const CenteredDiv = styled.div`
  font-family: Poppins;
  font-size: 12px;
  position: relative;
  width: 90px;
  margin: 0 auto;
`;

export default CenteredDiv;
