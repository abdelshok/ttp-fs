// Div that wraps the footer components (copyright and imprint) to center them.
import styled from 'styled-components';

const FooterBlock = styled.div`
  margin-top: 20px;
  text-align: center;
`;

export default FooterBlock;
