// Navigation Bar Link Logo
// Styled components that sets the height of the logo, its width, and its location within
// the parent (NavigationBarIconTextContainer) that contains it

import styled from 'styled-components';

const NavigationBarLinkLogo = styled.img`
  height: 80%;
  width: 20px;
  margin-right: 5px;
`;

export default NavigationBarLinkLogo;
