// Container that is used within the InputFieldAndIconContainer
// Used to group both the InputField and InputIcon together
// And ensure that they are centered correctly withinn the input
// field

// TO BE DELETED: Component is useless.

import styled from 'styled-components';

const InputFieldContainer = styled.div`
  padding-top: 20px;
`;

export default InputFieldContainer;
